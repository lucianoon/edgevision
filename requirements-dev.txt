# Tooling for development and the CI (on top of requirements.lock). shellcheck comes from the
# system package (preinstalled on the GitHub runner; `apt install shellcheck` elsewhere).
ruff==0.16.8
mypy==2.3.1
pytest-cov==7.1.0
